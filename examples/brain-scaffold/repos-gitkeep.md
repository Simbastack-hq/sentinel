# Per-repo engineering logs

One file per watched repo lands here, named `<repo>.md`, maintained by Sentinel's `brain-sync` agent
(see [`../README.md`](../README.md) and [`../_template.md`](../_template.md)). This file just keeps the
`repos/` directory present before the first repo file exists.
